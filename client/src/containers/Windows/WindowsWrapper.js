import styled from 'styled-components'

const WindowsWrapper = styled.aside`
  display: inline-block;
  width: 45rem;
  vertical-align: top;
  letter-spacing: normal;
`

export default WindowsWrapper
